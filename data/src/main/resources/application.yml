spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/workjh?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

logging.level:
  org.hibernate:
    SQL: debug
    type: trace

